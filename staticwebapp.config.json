{
  "routes": [
      {
        "route": "/resources/*",
        "rewrite": "/resources-meta.html"
      }
    ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
  }
}